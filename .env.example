# Environment Variables
# Copy this file to .env.local and fill in any values you need.
# This app works with ZERO environment variables - all are optional.

# =============================================================================
# APPLICATION
# =============================================================================

# Public app URL (used for absolute URLs in metadata)
# NEXT_PUBLIC_APP_URL=https://your-domain.com

# Override app name (defaults to siteConfig.siteName)
# NEXT_PUBLIC_APP_NAME=Your App Name

# =============================================================================
# FEATURE FLAGS
# =============================================================================

# Enable analytics integration (set to "true" to enable)
# NEXT_PUBLIC_ENABLE_ANALYTICS=false

# =============================================================================
# API KEYS (add your own as needed)
# =============================================================================

# Example: Analytics
# NEXT_PUBLIC_GA_TRACKING_ID=UA-XXXXXXXX-X

# Example: Payment processing (server-side only, no NEXT_PUBLIC_ prefix)
# STRIPE_SECRET_KEY=sk_test_xxxxx

# Example: Email service (server-side only)
# RESEND_API_KEY=re_xxxxx

# =============================================================================
# NOTES
# =============================================================================
# - Variables prefixed with NEXT_PUBLIC_ are exposed to the browser
# - Variables without the prefix are only available server-side
# - Never commit secrets to version control
# - Use Vercel environment variables for production secrets
